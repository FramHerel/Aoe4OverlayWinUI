<Page
    x:Class="Aoe4OverlayWinUI3.Views.GamesListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <controls:DataGrid
            AutoGenerateColumns="False"
            GridLinesVisibility="Horizontal"
            IsReadOnly="True"
            ItemsSource="{x:Bind ViewModel.Games, Mode=OneWay}">
            <controls:DataGrid.Resources>
                <SolidColorBrush x:Key="DataGridColumnHeaderBackgroundColor" Color="Transparent" />
            </controls:DataGrid.Resources>
            <controls:DataGrid.Columns>
                <!--  TODO: 添加翻译  -->
                <controls:DataGridTextColumn Binding="{Binding Result}" Header="Result" />
                <controls:DataGridTextColumn Binding="{Binding Map}" Header="Map" />
                <controls:DataGridTextColumn Binding="{Binding Civilization}" Header="Civilization" />
                <controls:DataGridTextColumn Binding="{Binding Duration}" Header="Duration" />
                <controls:DataGridTextColumn Binding="{Binding StartTime}" Header="StartTime" />
                <controls:DataGridTextColumn Binding="{Binding Kind}" Header="Kind" />
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <ProgressRing
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />

    </Grid>
</Page>
